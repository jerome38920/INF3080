a) ...

b) commit;

c) DELETE FROM Langue WHERE pLangue IN (SELECT pLangue FROM Langue MINUS (SELECT pLangue FROM XSession));

d) CREATE UNIQUE INDEX unique_index_courriel ON Usager(cCourriel);

e) UPDATE Usager SET cUsager = cUsager||'Impair' WHERE MOD(pUsager,2) <> 0;

f) 
SELECT COUNT(*) as total, cIP, cLangue FROM Historique h, Langue l 
WHERE h.pLangue = l.pLangue 
GROUP BY cIP,pLangue ORDER BY cLangue;


UPDATE Connexion SET cPassword = 'CeciEstUnBonMotDePasse?' WHERE LEN(cPassword) < 8;


ALTER TABLE Connexion ADD CONSTRAINT mdp_tres_secure CHECK (LEN(cPassword) > 8);

-- insert into, ca devrait marquer une erreur !


SELECT COUNT(*) FROM Connexion WHERE LEN(cPassword) < 7 UNION SELECT COUNT(*) FROM Connexion WHERE LEN(cPassword) = 8;
